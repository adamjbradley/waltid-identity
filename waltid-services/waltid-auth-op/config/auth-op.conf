issuer = "https://auth-op.theaustraliahack.com"
signingKeyPath = "config/signing-key.json"
# Flip to true in any non-dev deployment so the `sid` session cookie is only
# sent over HTTPS. Default is false for local ergonomics (curl/localhost).
cookieSecure = false
